<%= form_with(model: task_assignment) do |form| %>
  <% if task_assignment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(task_assignment.errors.count, "error") %> prohibited this task_assignment from being saved:</h2>

      <ul>
        <% task_assignment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :task_id, style: "display: block" %>
    <%= form.text_field :task_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
