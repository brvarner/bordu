<nav class="navbar navbar-expand-lg navbar-primary border-bottom border-dark mb-3">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Bordu</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse w-100" id="navbarSupportedContent">
            <ul class="navbar-nav mb-2 mb-lg-0 justify-content-end w-100 h-100">
                <% if user_signed_in? %>
                    <li class="nav-item d-flex align-items-center h-100"><%= link_to "My Projects", projects_path, class: "nav-link" %></li>
                    <li class="nav-item d-flex align-items-center h-100"><%= button_to 'Sign out', destroy_user_session_path, method: :delete, class: "nav-link" %></li>
                <% else %>
                    <li class="nav-item d-flex align-items-center h-100"><%= link_to 'Sign In', new_user_session_path, class: "nav-link" %></li>
                    <li class="nav-item d-flex align-items-center h-100"><%= link_to 'Sign Up', new_user_registration_path, class: "nav-link" %></li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>